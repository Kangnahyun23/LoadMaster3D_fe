# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# chỉ dùng pnpm — chặn lockfile của npm/yarn lọt vào repo
package-lock.json
yarn.lock

# thư mục dist build
dist

# kết quả Playwright (pnpm test:e2e)
test-results/
playwright-report/
blob-report/
# ảnh và nhật ký của công cụ Playwright MCP khi kiểm trên trình duyệt thật
.playwright-mcp/

# cấu hình và thư mục tạm của công cụ soạn thảo trên máy, không thuộc dự án
.claude/
CLAUDE.md

# gói bàn giao nén (nội dung đã giải nén vào design/, docs/)
docs/*.zip
/*.zip
